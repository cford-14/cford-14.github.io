const itemCompiler = (item, quantity, notes, imageFileName, arrayOfLists) => {
    return {
        item, //string
        quantity, //floor
        notes, //string
        imageFileName, //file name
        stores //array
    }
};

/*let master = masterList('crackers', 2, '', 'abcd.jpg', ['Walmart', 'Publix']);
console.log(master);

master.push(masterList('cookies', 3, '', '', ['Winn Dixie', 'Publix', 'Walmart']));
console.log(master);*/


const listMaker (item, quantity, notes, imageFileName, stores) => {
    masterList = []; //how do I import a saved list here?
    

}